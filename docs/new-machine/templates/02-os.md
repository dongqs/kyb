# 系统层 — {new-machine-remote-hostname}

我是谁：记录 remote:ssh {new-machine-ssh-user}@{new-machine-ssh-host} 的系统状态。
我在哪：remote:~/.claude/docs/02-os.md
我要干什么：描述 OS 版本、系统服务、容器环境。
我不干什么：不记录硬件、网络、运行时工具。

## 当前状态

> 📝 操作提示：每个子节按「命令执行协议」操作：local 写脚本 → scp → ssh 执行 → commit。

### OS 版本

```bash
# === local ===
cat > /tmp/kyb-{ts}-os-ver.sh << 'SCRIPT'
cat /etc/os-release
uname -a
hostname
uptime
SCRIPT
# === local→remote === scp && ssh && commit
```

| 项目 | 值 |
|------|-----|
| 操作系统 | Ubuntu / Debian / macOS / 其他______ |
| 版本 | 22.04 / 24.04 / 13 / Sequoia / 其他______ |
| 架构 | amd64 / arm64 |
| 内核 | ______ |
| 包管理器 | apt / brew / yum / pacman / 其他______ |
| 默认 shell | bash / zsh / fish / 其他______ |
| 运行时间 | ______ |
| 主机名 | ______ |

### 容器环境

```bash
# === local ===
cat > /tmp/kyb-{ts}-os-container.sh << 'SCRIPT'
cat /proc/1/cgroup | head -5
test -f /.dockerenv && echo "dockerenv found"
cat /proc/1/sched | head -1
SCRIPT
# === local→remote === scp && ssh && commit
```

| 项目 | 值 |
|------|-----|
| 在容器内 | Y / N |
| 容器引擎 | Docker / Podman / 其他______ / 不适用 |
| 容器名/ID | ______ |
| 镜像 | ______ |

### 确认

我确认以上 OS 信息正确，过程已记录。
签名：kyb-{new-machine-remote-hostname}  日期：________ (精确到秒)

## 系统服务

> 📝 操作提示：逐个过，每过 5 个 commit 一次。用 audit 脚本批量查：

```bash
# === local ===
cat > /tmp/kyb-{ts}-os-services.sh << 'SCRIPT'
for svc in sshd docker containerd tailscaled NetworkManager cron cups bluetooth avahi-daemon postgresql nginx; do
  echo -n "$svc: "
  systemctl is-active "$svc" 2>/dev/null || echo "not found"
done
SCRIPT
# === local→remote === scp && ssh && commit
```

| 服务 | 已装 | 运行中 | 用途 |
|------|------|--------|------|
| sshd | Y / N | Y / N | SSH 远程登录 |
| docker | Y / N | Y / N | 容器引擎 |
| containerd | Y / N | Y / N | 容器运行时 |
| tailscaled | Y / N | Y / N | Tailscale VPN |
| NetworkManager | Y / N | Y / N | 网络管理 |
| systemd-networkd | Y / N | Y / N | 网络管理 |
| systemd-resolved | Y / N | Y / N | DNS 解析 |
| cron / systemd-timers | Y / N | Y / N | 定时任务 |
| cups | Y / N | Y / N | 打印服务 |
| bluetooth | Y / N | Y / N | 蓝牙 |
| avahi-daemon | Y / N | Y / N | mDNS / .local 解析 |
| postgresql | Y / N | Y / N | PostgreSQL 数据库 |
| mysql / mariadb | Y / N | Y / N | MySQL 数据库 |
| nginx / apache2 | Y / N | Y / N | Web 服务器 |
| smartmontools | Y / N | Y / N | 磁盘 S.M.A.R.T. 监控 |
| ufw | Y / N | Y / N | 防火墙 |
| samba | Y / N | Y / N | 文件共享 |
| redis | Y / N | Y / N | Redis 缓存 |
| ntp / chrony / systemd-timesyncd | Y / N | Y / N | 时间同步 |
| syslog / rsyslog / systemd-journald | Y / N | Y / N | 日志 |
| 其他______ | Y / N | Y / N | ______ |

### 确认

我确认以上系统服务信息正确，过程已记录。
签名：kyb-{new-machine-remote-hostname}  日期：________ (精确到秒)

## 探索日志

| # | 时间 (精确到秒) | 操作 | 结果 | 耗时 | commit |
|---|------|------|------|------|--------|
| 1 | | OS 版本信息 | 成功/失败 | __s | |
| 2 | | 容器环境检测 | 成功/失败 | __s | |
| 3 | | 系统服务清单 | 成功/失败 | __s | |

## 最终复查

我已复查整个文档，所有操作已记录并 commit。

总耗时：____ 分钟
签名：kyb-{new-machine-remote-hostname}  日期：________ (精确到秒)
