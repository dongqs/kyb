# docs/infra/grafana/provisioning/datasources/prometheus.yaml
# Prometheus datasource — container-level metrics from cAdvisor and postgres_exporter
# Status: FUTURE — Prometheus not yet deployed. This file is prepared for Phase 4 integration.
# See: docs/infra/reviews/cadvisor-metrics.md, docs/infra/reviews/prometheus-scrape.md
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://kyb-infra-prometheus:9090
    isDefault: false
    editable: false
