# Server: 192.168.215.12

**Alias**: 26869595dd28 (container hostname)
**Provisioned**: pre-2026-05-20 (uptime >7 days as of 2026-05-21)
**Status**: active, fresh (no projects deployed)
**Type**: kyb sandbox container (Docker on OrbStack)

---

## Physical / Container Layer

| Item | Value |
|------|-------|
| Platform | OrbStack (macOS 虚拟化) |
| Type | Docker 容器 |
| 容器主机名 | 26869595dd28 |
| 架构 | aarch64 (ARM64) |
| CPU | Apple M-series, 10 vCPU @ 2.0 GHz |
| 内存 | 15 GiB (5.3Gi used, 10Gi avail) |
| Swap | 16 GiB (zram0: 15.7G lz4, 2.4G used; /dev/vdc: 1G) |
| 磁盘 | 78G overlay (53G used, 67%) |
| 存储驱动 | overlay (overlayfs) |
| PID 1 | `sleep infinity` |

机器为 **Docker 容器**，运行在 OrbStack 宿主机上。内核版本与主 kyb 沙箱 (`192.168.215.5`) 完全相同。

## OS

| Item | Value |
|------|-------|
| 发行版 | Ubuntu 24.04.4 LTS (Noble Numbat) |
| 内核 | Linux aarch64, 7.0.5-orbstack-00330-ge3df4e19b0a0-dirty |
| 时区 | Etc/UTC |
| Locale | C.UTF-8 |
| 用户 | dev (uid=1000, groups: dev, sudo) |
| Sudo | NOPASSWD (ALL) |
| Shell | /bin/bash (5.2.21) |
| 运行时间 | 7 天 22 小时 |

**关键 APT 包**: containerd, cron, bind9-dnsutils, bridge-utils, dnsmasq-base, gnupg, build-essential (gcc/g++ 13.3), dbus

## Network

| Item | Value |
|------|-------|
| IP | 192.168.215.12/24 |
| 网关 | 192.168.215.1 |
| 接口 | eth0@if1974 (veth pair) |
| MAC | c2:5f:7c:c5:9c:7e |
| DNS | 0.250.250.200 (Docker 生成) |
| 子网 | 192.168.215.0/24 (OrbStack 内网) |

**开放端口**: 22 (SSH) 仅此一个。
**防火墙**: 无 (iptables/ufw 未配置)。
**代理**: 未配置。
**Tailscale**: 未安装。
**kubectl**: 未安装。

与主 kyb 沙箱 `192.168.215.5` 在同一 OrbStack overlay 网络中。

### 连通性

| 目标 | 结果 |
|------|------|
| baidu.com | 可达 (301, ~0.20s) |
| git.leyantech.com | 可达 (302, ~0.13s) |
| google.com | 不可达 (timeout, 无代理) |

## 运行时 / 工具链

全部由 **mise** (v2026.5.12 linux-arm64) 管理：

| 工具 | 版本 | 安装方式 |
|------|------|---------|
| mise | 2026.5.12 | 系统 |
| Ruby | 3.3.11 | mise |
| Node.js | 25.9.0 | mise |
| Python | 3.10.20 | mise |
| Maven | 3.9.16 | mise (但无 JDK) |
| glab | 1.92.1 | mise |
| clickhouse-client | 26.4.2.10 | mise |
| claude-code | 2.1.145 | mise (npm) |
| git | 2.43.0 | 系统 |
| OpenSSH | 9.6p1 | 系统 |
| Docker | 29.1.3 | 系统 (client only; daemon via host socket) |
| Docker Compose | 2.40.3 (plugin) | 系统 |
| Docker Buildx | 0.30.1 | 系统 |
| containerd | 2.2.1 | 系统 |
| Bash | 5.2.21 | 系统 |
| npm | 11.12.1 | mise |
| pip | 26.1.1 | mise |
| gem | 3.5.22 | mise |
| jq | 1.7 | 系统 |
| htop | 3.3.0 | 系统 |
| vim | 9.1 | 系统 |
| curl | 8.5.0 | 系统 |
| wget | 1.21.4 | 系统 |
| make | 4.3 | 系统 |
| gcc/g++ | 13.3.0 | 系统 |

### 缺失工具

| 工具 | 状态 |
|------|------|
| Java | 未安装 |
| Go | 未安装 |
| Rust | 未安装 |
| kubectl | 未安装 |
| helm | 未安装 |
| tailscale | 未安装 |
| yq | 未安装 |
| tmux | 未安装 |
| nvim | 未安装 |
| rsync | 未安装 |
| cmake | 未安装 |
| docker-compose (独立 binary) | 未安装 (`docker compose` plugin 可用) |

### Docker 状态

Docker client 可用且通过宿主机 socket 连接 daemon。当前无运行中容器，无已拉取镜像。

## 服务

| 服务 | 状态 | 端口 |
|------|------|------|
| SSH (OpenSSH) | 运行中 | 22 (PubkeyAuthentication) |
| Docker daemon | 仅 client | - |
| containerd | 运行中 | - |
| cron | 已安装无任务 | - |

无其他服务运行。无 PostgreSQL、ClickHouse、Web 服务器。

## 用户账户

| 用户 | 组 | Sudo | Auth |
|------|----|------|------|
| dev | dev, sudo | passwordless (NOPASSWD) | SSH key (dongqs@S.local) |

## Claude Code 环境

`~/.claude/` git 仓库，2 commits:
- `b26ab9f` init
- `e2e07a0` add record.sh helper

包含 `audits/` 和 `docs/` 目录，以及 `record.sh` 辅助脚本（用于远程探测和自动 commit 审计记录）。

## 项目

无。`~/projects/` 目录为空。这是一个**初始状态**的沙箱。

## 观察

1. **与主 kyb 沙箱一致** -- 使用相同镜像、相同工具链、相同内核。推测是同一台 OrbStack 宿主机上的另一个 kyb 沙箱容器。
2. **初始状态** -- 无项目、无 Docker 镜像、无运行中服务。
3. **无 Tailscale** -- 如需跨网络访问，需安装并配置 Tailscale。
4. **无 Java** -- Maven 已安装但缺少 JDK，`java` 命令不可用。
5. **无代理** -- `http_proxy`/`https_proxy` 未设置（与主 kyb 容器不同，主容器有 `socks5://host.orb.internal:2080`）。
6. **外网不通** -- 无代理导致 google.com 等外网不可达。
7. **SSH Host Key 漂移** -- 容器可能因重建导致 host key 变化，连接时会冒 host key verification failed。
