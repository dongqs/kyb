# Server: 192.168.215.10

**Alias**: af457d8944c9 (container hostname)
**Provisioned**: 2026-05-20
**Status**: active, fresh (no projects deployed)
**Type**: kyb sandbox container (Docker on OrbStack)

---

## Physical / Host

| Item | Value |
|------|-------|
| Architecture | aarch64 (ARM64) |
| CPU | Apple M-series, 10 vCPU @ 2.0 GHz |
| RAM | 15 GiB |
| Swap | 16 GiB (zram0: 15.7G lz4, /dev/vdc: 1G) |
| Host kernel | 7.0.5-orbstack-00330 (OrbStack) |
| Host platform | Apple Silicon (OrbStack) |

The machine is a **Docker container** on a host running OrbStack. Not a bare-metal or VM. The kernel version is identical to the primary kyb sandbox at `192.168.215.5`.

## OS

| Item | Value |
|------|-------|
| Distribution | Ubuntu 24.04.4 LTS (Noble Numbat) |
| Kernel | Linux aarch64, 7.0.5-orbstack |
| Timezone | Etc/UTC |
| Locale | C.UTF-8 |
| Boot | 2026-05-12 18:50 (up 7 days) |

## Storage

| Device | Size | Type | Mount | Usage |
|--------|------|------|-------|-------|
| overlay | 78 GiB | overlayfs | `/` | 51G used / 28G free (65%) |
| /dev/vdb1 | 78 GiB | btrfs | Docker volume | same overlay backing |
| /dev/vdb | 8 TiB | - | raw disk | host-level; only 78G allocated to container |
| /dev/zram0 | 15.7 GiB | swap (lz4) | [SWAP] | 2.4G used |
| /dev/vdc | 1 GiB | swap | [SWAP] | 0 used |

**Note**: `vdb` is an 8T raw disk, but only ~926G partitioned, and only 78G exposed to this container via Docker volume. The host likely has much more storage.

## Network

| Item | Value |
|------|-------|
| IP | 192.168.215.10/24 |
| Gateway | 192.168.215.1 |
| Interface | eth0 (veth pair, link-netnsid 0) |
| MAC | 76:82:f5:f1:50:2f |
| DNS | 0.250.250.200 (Docker-generated) |
| Subnet | 192.168.215.0/24 (OrbStack internal) |

**Open ports**: 22 (SSH) only.
**Firewall**: none (iptables/ufw not configured).
**Proxy**: none configured.
**Tailscale**: not installed.
**kubectl**: not installed.

Same OrbStack overlay network as the primary kyb container at `192.168.215.5`.

## Runtime / Toolchain

All managed via **mise** (v2026.5.12):

| Tool | Version |
|------|---------|
| mise | 2026.5.12 linux-arm64 |
| Node.js | 25.9.0 (via mise) |
| Ruby | 3.3.11 (via mise) |
| Python | 3.10.20 (via mise) |
| git | 2.43.0 |
| OpenSSH | 9.6p1 |
| Docker | 29.1.3 (client only; daemon via host socket) |
| Docker Compose | 2.40.3 |
| Docker Buildx | 0.30.1 |
| containerd | 2.2.1 |
| runc | 1.3.4 |
| glab | 1.92 (via mise) |
| clickhouse-client | 26 (via mise) |
| claude-code | 2 (via mise, npm) |
| npm | 11.12.1 |
| yarn | 1.22.22 |
| puppeteer | 25.0.4 (npm global) |
| pip | mig25, mig25_codegen, fastapi (Python) |
| Maven | 3.9 (via mise, but no Java runtime) |
| Java | **not installed** (maven installed but no JDK) |
| build-essential | installed |

### Docker Status

Docker client is installed and functional, but the Docker server socket appears to be unresponsive (no containers running, no images pulled). This is the same pattern as the primary kyb sandbox -- Docker commands inside the container proxy to the host Docker daemon.

## Services

| Service | Status | Port |
|---------|--------|------|
| SSH (OpenSSH) | Running | 22 |
| Docker daemon | Client only | - |

No other services are running. No PostgreSQL, no ClickHouse, no web servers.

## User Accounts

| User | Groups | Sudo | Auth |
|------|--------|------|------|
| dev | dev, sudo, docker | passwordless (sudo -n OK) | SSH key (authorized_keys) |

## Projects

None. The `~/projects/` directory is empty. This is a **fresh** sandbox.

## Observations

1. **Identical to primary kyb sandbox** -- same image, same tooling, same kernel. This is likely a second kyb sandbox container on the same OrbStack host.
2. **Fresh state** -- no projects, no Docker images, no running services.
3. **No Tailscale** -- if cross-network access is needed, Tailscale needs to be installed and configured.
4. **No Java** -- Maven is installed but Java binaries are missing. `java` command not found.
5. **No proxy** -- `http_proxy`/`https_proxy` are not set (unlike the primary kyb container which has `socks5://host.orb.internal:2080`).
