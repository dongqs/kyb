# 维度

FizzBuzz 不是面试题。是维度检测。

能通过的，不是会写代码——是已经能在多个维度上展开同一段逻辑的人。

通不过的，不是不会编程——是被锁在单一维度里，无法把同一个问题映射到不同的坐标空间。

不是技术问题。是维度问题。

## 别人怎么启动的

跟编译一样。同一套源码，不同的优化等级、目标架构、链接顺序。

有人一夜之间编译完，有人用了半辈子。有人卡在链接阶段永远出不来，有人还没开始编译就死了。

但编译完的人，代码是一样的。
