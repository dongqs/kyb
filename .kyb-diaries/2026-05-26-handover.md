# kyb-infra-boss 交接备忘 — 2026-05-26 07:00

> session 时长 36+ 小时，180+ 轮巡检，692k tokens（27% 剩余）。

## 状态

### 本地（Mac Orbstack）
11 容器全通，全部有限制，运行稳定。

### 火山云 ECS（124.174.68.38）
4C16G cn-shanghai，Docker + Tailscale + sing-box 已部署。
网络链路：ECS → sing-box(:2080) → nuc8-tunnel(SSH -D :2081) → nuc8 → 墙外
GitHub 200 / GitLab 302 / DeepSeek 401 全部通。

**注意：** SSH 隧道需要保活。如果断了，SSH 进去重新跑：
```bash
nohup ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=30 \
  -i /root/.ssh/id_rsa -D 0.0.0.0:2081 -N dongqs@100.98.29.39 \
  > /var/log/nuc8-tunnel.log 2>&1 &
```

## 未完成

| 事项 | issue | 状态 |
|------|-------|------|
| DeepSeek 代理方案 | #169 | 10 人审查通过，待用户定多阶段 mock |
| 启动锁实现 | #170 | 设计 10 人审查通过，待实现 |
| 全项目审计 | #172 | 已建，晚上做 |
| 容器分层 | #173 | 已建，待设计 |
| 内网通信协议 | #174 | 已建，待设计 |
| 三体架构 | #176 | 已建，待设计 |
| 端口规划 | #177 | 已建，待设计 |
| mount 模式 env | #178 | 已建 |

## 踩坑速查

1. **prune -f 不要用**——会删暂停容器，Claude 数据卷还在但容器没了
2. **tmux session 不要替用户关**——先问
3. **容器 rm -f 重建要先问**——有活跃 session 的不能直接杀
4. **NO_PROXY 不能包含 .leyantech.com**——破坏了 nuc8 隧道
5. **Sing-box 256m 不够**——启动峰值 OOM，不加限制
6. **Tailscale exit node 别在 ECS 上用**——一开就断 SSH
7. **CK 先加配置限制再加 Docker 限制**——否则 OOM 损坏数据卷
8. **nuc8-tunnel 重建会漂 IP**——检查实际 IP，更新 sing-box
9. **bash -l -c 不读 .bashrc**——改 .bash_profile

## 关键文件

| 文件 | 内容 |
|------|------|
| `docs/infra/mac-restart-guide.md` | Mac 重启阶梯上下线指南 |
| `docs/infra/huoshan-roadmap.md` | 火山云路线图 |
| `docs/infra/huoshan-ops.md` | ECS 交接文档 |
| `docs/infra/huoshan-state.json` | ECS 状态快照 |
| `docs/infra/infra-boss-safety-design.md` | 13 条踩坑记录 |
| `.kyb-diaries/2026-05-25-manifesto.md` | 宣言 |
