# 2026-05-27 — 权限已全开

问 Claude 怎么跳过 dangerous permissions，查了 help 发现有 `--dangerously-skip-permissions`、`--permission-mode bypassPermissions`、`--allowedTools` 几个选项。

准备改 settings 的时候看了一眼——早就全开了：

- `permissions.allow: ["*"]` — 所有工具静默通过
- `skipDangerousModePermissionPrompt: true` — 危险模式弹窗已关

所以一直在全开状态，不需要加任何 flag。试了一发验证：Bash 直跑不弹窗。

教训：先查现状再决策，不从零开始。已经做过的事别再做第二次。
