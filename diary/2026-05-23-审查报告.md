# Full Review Report — 2026-05-23

> 跨团队并行审计结果。5 个维度 × 1 agent = 5 路并发。

---

## 1. Security Review

**审计范围**: `infra.rb`, `docker.rb`, `cli.rb`, `container.rb`, `entrypoint.sh`, `config.rb`, `parser.rb`, `create.rb`, `enter.rb`, `manage.rb`

### HIGH — 3 项

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 1 | **Docker socket 直通 → 容器逃逸** | `docker.rb:190`, `infra.rb:99` | 文档标注 trusted-workload 威胁模型 |
| 2 | **API Key 通过 `-e` 传环境变量** | `docker.rb:154-158`, `infra.rb:65`, `enter.rb:140` | 改用 Docker secrets 或 file-based 传参 |
| 3 | **Heredoc 未引用 → 命令注入** | `entrypoint.sh:129` | `<< YAML` → `<< 'YAML'`（**一行修复**） |

### MEDIUM — 4 项

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 4 | Branch 名无输入校验 | `parser.rb:28` | 加 regex 白名单 |
| 5 | 用户可配任意 host 路径挂载 | `docker.rb:213-218` | 限制 host_path 在 `$HOME` 下 |
| 6 | Proxy URL 可能泄露凭据 | `entrypoint.sh:210-211` | strip userinfo 再输出 |
| 7 | 反引号执行模式脆弱 | 多处 | 逐步迁移到 `Open3.capture2` |

### LOW — 4 项

8. PostgreSQL 密码明文在文档中
9. KIMI_API_KEY 在 `ps aux` 可见
10. 无容器创建限流
11. glab config 路径 macOS 专用 → Linux 上 Token 自动加载失效

### 正面发现

- `Shellwords.escape` 在 `enter.rb:126`, `infra.rb:161` 正确使用
- 所有 `system()` 使用数组形式传参
- SSH key `chmod 600`, glab config `chmod 600`
- 无 `--privileged` / `--cap-add`
- 敏感路径全部 `:ro` 挂载
- `YAML.safe_load_file` 无 Psych RCE 风险
- `--model`, `--cli` 参数有白名单校验

---

## 2. Architecture Review

**审计范围**: `lib/kyb/*.rb`, `bin/kyb`, `README.md`

| # | 类型 | 问题 | 建议 |
|---|------|------|------|
| 1 | **Bug** | `doctor` 命令完全实现但未挂到 CLI 分发器 | wire 进 dispatch |
| 2 | **Smell** | infra-boss 绕过 `Kyb::Docker` 重复造轮子 | 复用 Kyb::Docker |
| 3 | Dead code | `GRADLE_CACHE_VOLUME` / `MAVEN_CACHE_VOLUME` 常量 | 删除 |
| 4 | Dead code | `Container#home_volume`, `create_diverse` | 删除 |
| 5 | Dead code | `sandbox_allowed_domains` 配置项 | 删除 |
| 6 | Copy-paste | project/branch 解析在 `cli.rb` 重复 5 次 | 抽 parsing helper |
| 7 | Duplication | ExitFlow / rm / prune 三处独立 DID 清理逻辑 | 去重 |

### worktree→mount 迁移

干净，无 `git.rb` / `sandbox.rb` 残留。

---

## 3. Code Quality Review

**审计范围**: `lib/kyb/*.rb`, `test/*.rb`, `.gitlab-ci.yml`

| # | 严重度 | 问题 |
|---|--------|------|
| 1 | **HIGH** | `create_diverse` 定义在顶层（不在 module 内），零测试 |
| 2 | **HIGH** | `check_proxy` 早期失败时潜在 NameError |
| 3 | **HIGH** | CI 无覆盖率测量 |
| 4 | **HIGH** | 大量公共方法零测试：`ps`, `prune`, `infra_enter`, `did_create` 全流程, `notify`, `exec_cmd`, `create_diverse` |
| 5 | MEDIUM | ~30% 测试在 CI 中被跳过（Docker/container 测试） |
| 6 | MEDIUM | 4 个网络相关测试会 flake |
| 7 | MEDIUM | `stale?` 方法静默吞所有异常 |
| 8 | LOW | `require_relative` 放置不惯用 |

**建议**: 加 SimpleCov 做覆盖率度量，修 `create_diverse` module 作用域。

---

## 4. Infrastructure Review

**审计范围**: `Dockerfile`, `entrypoint.sh`, `.gitlab-ci.yml`, `mise.config.toml`

> **⚠️ 未收到审稿人详细报告，仅从任务标记判断已完成。**

---

## 5. Branch Audit

**审计结果**: 19 分支全部未合并入 master。

#### 已删除远程分支

```
kyb/fix-rm-cname            # rm cname bug 修复
kyb/fix-stale-hang          # Docker stale 哈希检测
kyb/ci-fix-v2               # module_function 修复
kyb/issue-cli-batch         # doctor/onboard/event 命令
kyb/morning-command         # kyb morning 命令
kyb/auto-metrics            # CK 自动 metrics
kyb/worldview-system        # agent worldview 框架
kyb/dispatch-chain-docs     # 文档
kyb/scaling-pattern-doc     # 文档
kyb/update-template         # 模板更新
kyb/onboarding-manager-doc  # 文档
kyb/onboarding-template-update    # 模板
kyb/onboarding-template-update2   # 模板
kyb/onboarding-template-v2        # 模板
kyb/progress-summary        # 文档
kyb/fix-claude-postinstall  # CI 修复（已合并但 remote 未删）
kyb/diary-2026-05-22        # 日记
notify-design               # TTS notify 功能（remote 已不存在）
ignore                       # 测试（remote 已不存在）
issue-19-auto-detect        # 自动检测（remote 已不存在）
```

---

## 优先级建议

| P | 项目 | 工作量 |
|---|------|--------|
| **P0** | Heredoc 注入修复 `entrypoint.sh:129` | ~1 行 |
| **P1** | wire doctor 命令到 CLI 分发器 | 小 |
| **P1** | 抽 parsing helper（去 5 处重复） | 中 |
| **P1** | 加 SimpleCov + 补关键路径测试 | 中 |
| **P2** | 环境变量传参改文件传参 | 中 |
| **P2** | 加 branch 名输入校验 | 小 |
| **P3** | 死代码清理 | 小 |
| **P3** | infra-boss 重构复用 Kyb::Docker | 大 |
| **P3** | 清理逻辑三合一 | 中 |

---

_Generated by full-review team — 5 reviewers parallel, 2026-05-23_
